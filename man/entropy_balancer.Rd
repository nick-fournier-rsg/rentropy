% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{entropy_balancer}
\alias{entropy_balancer}
\title{Entropy Balancer}
\usage{
entropy_balancer(
  incidence,
  initial_weights,
  control_totals,
  weights_lb = as.numeric(c()),
  weights_ub = as.numeric(c()),
  controls_importance = as.numeric(c()),
  master_control_index = -1L,
  max_iterations = 10000L,
  max_delta = 1e-08,
  print_every_n = 10L
)
}
\arguments{
\item{incidence}{Numeric matrix of incidence data (samples x controls).}

\item{initial_weights}{Numeric vector of initial weights for each sample.}

\item{control_totals}{Numeric vector of target totals for each control.}

\item{weights_lb}{Numeric vector of lower bounds for weights (optional).}

\item{weights_ub}{Numeric vector of upper bounds for weights (optional).}

\item{controls_importance}{Numeric vector of importance weights for each control (optional).}

\item{master_control_index}{Integer index of the master control (optional, default -1).}

\item{max_iterations}{Maximum number of iterations to run (default 10000).}

\item{max_delta}{Maximum allowed change in weights for convergence (default 1e-9).}

\item{print_every_n}{Print progress every n iterations (default 10).}
}
\value{
A list containing the final weights, relaxation factors, convergence status, number of iterations, delta, and max gamma difference.
}
\description{
Perform entropy balancing on a given incidence matrix with specified control totals and weights.
}
