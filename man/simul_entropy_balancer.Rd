% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{simul_entropy_balancer}
\alias{simul_entropy_balancer}
\title{Simultaneos Entropy Balancer}
\usage{
simul_entropy_balancer(
  incidence,
  sub_weights,
  sub_controls,
  parent_controls,
  parent_weights,
  weights_lb,
  weights_ub,
  controls_importance,
  master_control_index = -1L,
  max_iterations = 10000L,
  max_delta = 1e-08,
  print_every_n = 10L
)
}
\arguments{
\item{incidence}{A matrix of incidence values with dimensions (control_count × sample_count).}

\item{sub_weights}{A matrix of weights with dimensions (zone_count × sample_count) (mutable).}

\item{sub_controls}{A matrix of control values with dimensions (zone_count × control_count).}

\item{parent_controls}{A vector of parent control values with length (control_count).}

\item{parent_weights}{A vector of parent weights with length (sample_count).}

\item{weights_lb}{A vector of lower bounds for weights with length (sample_count).}

\item{weights_ub}{A vector of upper bounds for weights with length (sample_count).}

\item{controls_importance}{A vector of importance values for each control with length (control_count).}

\item{master_control_index}{An integer index for the master control, -1 means no master control.}

\item{max_iterations}{An integer specifying the maximum number of iterations to perform.}

\item{max_delta}{A double specifying the maximum allowed change in weights for convergence.}

\item{print_every_n}{An integer specifying how often to print progress information.}
}
\value{
A list containing the updated weights, relaxation matrix, convergence status, number of iterations, delta, max gamma difference, and elapsed time.
}
\description{
This function performs simultaneous entropy balancing on a set of samples across multiple zones, i.e., 2D balancing.
}
